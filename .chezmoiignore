# Keep in repo but don't apply
LICENSE
README.md

# Local-only, untracked (see .gitignore) - never apply
TODO.md

# macOS-only ignores (Linux files to skip on macOS)
{{ if eq .chezmoi.os "darwin" }}
.vim/python.vim
{{ end }}

# Linux-only ignores (macOS files to skip on Linux)
{{ if eq .chezmoi.os "linux" }}
{{ end }}
